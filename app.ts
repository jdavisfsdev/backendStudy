// Practicing with types

const greeting: string = 'hello';

// Type array of numbers
const numbers: number[] = [1,2,3];


// Solves 'Cannot redeclare block-scoped vars error
// Tells TS our file is a module with its own scope
export {}